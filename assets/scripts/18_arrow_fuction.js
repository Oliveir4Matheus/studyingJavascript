// Arrow function - são funções anônimas que permitem escrever uma função com menos código sem perder a clareza do código.

// Ele pode ser usada em contextos onde uma função realiza poucas instruções ou em contextos de callback.

let menssage = (menssage) => {
        new Error("adsfdasgf")
        console.log(`-------------- ${menssage}-------------------`)
}

console.log(menssage("texto"))